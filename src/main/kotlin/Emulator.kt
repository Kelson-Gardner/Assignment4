class Emulator {
    private val cpu = CPU
    private var rom = ROM
    private var ram = RAM

    fun loadAndRunProgram(path: String) {
//        cpu.loadProgram(path)
//        cpu.start()
    }


    fun stopProgram() {
//        cpu.stop()
    }
}